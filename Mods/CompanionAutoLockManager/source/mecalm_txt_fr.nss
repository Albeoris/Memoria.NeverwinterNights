void main()
{
    int iKey = StringToInt(GetScriptParam("MECALM_TEXT_KEY"));
    string sText = iKey == 1 ? "Crochetage automatique" : iKey == 2 ? "Activez cet objet pour activer ou désactiver le crochetage automatique par les compagnons. Le gestionnaire de sécurité qui empêche les compagnons d'attaquer les serrures et les pièges manipulés reste toujours actif." : iKey == 3 ? "Crochetage automatique : activé." : iKey == 4 ? "Crochetage automatique : désactivé." : iKey == 5 ? "Le crochetage automatique est suspendu pendant que vous contrôlez directement votre familier." : iKey == 6 ? "Aucun compagnon n'a pu atteindre la serrure ; tâche abandonnée :" : iKey == 7 ? "Gestionnaire de crochetage automatique installé, version" : iKey == 8 ? "Diagnostic MECALM : serrures visibles admissibles = " : iKey == 9 ? ", crocheteurs disponibles = " : iKey == 10 ? "ACTIF" : iKey == 11 ? "INACTIF" : iKey == 12 ? "Je vais crocheter cette serrure !" : iKey == 13 ? "Gestionnaire de crochetage automatique" : iKey == 14 ? "Crochetage automatique" : iKey == 15 ? "Activé" : iKey == 16 ? "Rayon de recherche (1-100 m)" : iKey == 17 ? "Intervalle visibilité/tâches (0.1-6 s)" : iKey == 18 ? "Exiger une ligne de vue" : iKey == 19 ? "Empêcher les attaques sur les serrures traitées" : iKey == 20 ? "Surbrillance grise des serrures détectées" : iKey == 21 ? "Messages au-dessus des compagnons" : iKey == 22 ? "Sortie de débogage" : iKey == 23 ? "Mode Détection automatique" : iKey == 24 ? "Intervalle de mouvement (0.1-6 s)" : iKey == 25 ? "Délai d'immobilité (0.1-30 s)" : iKey == 26 ? "Enregistrer" : iKey == 27 ? "Annuler" : iKey == 28 ? "Valeur incorrecte. Respectez les plages indiquées." : iKey == 29 ? "Paramètres MECALM enregistrés." : "";
    SetLocalString(OBJECT_SELF, "MECALM_TEXT_RESULT", sText);
}
